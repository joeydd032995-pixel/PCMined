{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability set. CRITICAL: the webview is granted NO ability to execute processes. All miner spawning happens behind typed Rust commands using tokio::process. The shell plugin is limited to opening URLs/paths (shell:allow-open) only — shell:allow-execute is intentionally absent.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "core:app:allow-version",
    "shell:allow-open",
    "notification:default",
    "os:allow-platform",
    "os:allow-version",
    "os:allow-arch"
  ]
}
